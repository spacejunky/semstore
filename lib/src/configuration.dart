/// Configuration options for the combined datastore
class Configuration {

  /// whether or not to use cloud_firestors
  static bool useFirestore = true;

  /// whether the sembast database should be persistent or in-memory only
  static bool sembastPersistent = true;
}